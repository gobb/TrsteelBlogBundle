{% extends 'TrsteelAdminBundle::base.html.twig' %}

{% block trsteel_admin_content %}
<h1>{% block title %}Add Category{% endblock %}</h1>

<form action="{{ path('trsteel_blog_backend_category_add') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('trsteel_blog_backend_category_index') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}